# 工作日志 — 2026年7月10日

## 概述

完成抖音 116 关键词全量数据采集 → 爬取 → 评论拉取 → AI 标签 → 深度清洗 → 规范输出完整闭环。修复 CDP 搜索复用 bug、评论字段截断 bug。最终产出 7 份规范化数据文件。

## 时间线

### 09:00 - 11:00 | 116 关键词全量搜索

- **问题**：搜索 API 持续风控 (`verify_check`)，Cookie 有效但 API 不可用
- **解决**：修复 CDP 浏览器搜索，每个关键词独立 CDP 连接（避免 WebSocket 长连接不稳定），失败关键词自动重试
- **修改**：`source/douyin_crawler/spiders/search.py`
  - `_search_cdp()` 支持外部传入 CDP 连接 + 跳过导航标记
  - `search_multi()` 首个关键词确认 API 风控后，后续全走 CDP，失败词自动重试
  - 每关键词独立 CDP 连接 → 每个都 `Page.navigate` + `sleep(5)` 确保会话建立
- **结果**：116 关键词 → 1937 条唯一视频（关键词覆盖率 ~80%）

### 11:00 - 14:00 | 全量爬取基础数据

- 调用 `main.py` 爬取 1937 条视频的标题/文案/互动数据
- 成功 1718 条（88.7%），不含评论
- 耗时 ~7 小时（每条约 7-8 秒，含 API 限流延迟）

### 14:00 - 16:00 | 评论全量拉取

- 新建 `source/cleandata/fetch_comments_only.py`：读取已有 CSV，仅调 `fetch_comments` API 追加评论列
- 1718 条视频 → 1502 条有评论（87.4%）→ 20,496 条评论
- 耗时 ~4 小时（每条约 4-8 秒）
- 命令：`--comment-pages 5`（最多 250 条评论/视频）

### 16:00 | 重跑 AI 标签（含评论版）

- 删除无评论版旧标签，替换为 `爬取结果_全量_含评论.csv`
- 重跑 `douyin_labeler.py --workers 8`，8 维标签
- **对比**：正面情绪 358→770 (+115%)，混合情绪 210→640 (+205%）

### 16:00 - 17:00 | 数据深度清洗

- **第一轮** `clean_douyin.py`：去空/NSFW/无关"杯"/零互动/纯标签 → 2,405→2,126 (-279)
- **第二轮** `clean_douyin_v2.py`：
  - 过滤 AI 判定"不相关"的视频（含模糊匹配防误删）
  - 深度评论清洗（纯表情/哈哈哈/纯@/纯数字/重复字符/纯标点）
  - 2,126→1,087 条

### 17:00 | 修复评论字段截断 bug

- **根因**：`douyin_labeler.py` 的 `build_output()` 只保留前 5 条评论，且缺时间/属地/子回复
- **修复**：
  - `range(1, 6)` → `range(1, 21)`
  - 补充评论时间/属地/子回复字段
  - 列名从第一行 keys → 全量 keys 去重合并
- **效果**：评论从 3,623 条 → 10,550 条（322 列）

### 17:00 - 18:00 | 规范输出 + 归档

- 新建 `source/cleandata/final_output.py`：统一生成规范命名的最终文件
- 命名格式：`数据清洗结果_v{版本}_{时间范围}_{视频数}条_{评论数}评_{日期}.xlsx`
- 每个 Excel 双 sheet：汇总（8板块）+ 数据明细（全字段）
- 新增精选版：近三年 + 中高相关性 + 负面情绪专题 sheet

- 新建 `source/cleandata/validate.py`：8 项严谨性校验（完整性/类型/一致性/去重/标签/评论/匹配/分布）
- 新建 `progress.py`：实时进度监控工具

- 目录整理：

```
res/data/douyin/output/
├── 数据清洗结果_20260710/       ← 最终产出
│   ├── ...全量_663条_6819评...    全量
│   ├── ...近三年_575条_5889评...  近三年
│   ├── ...三年前_88条_930评...    三年前
│   └── ...中高相关性_含负面...    精选（含负面情绪专题）
├── 中间数据/                    ← 过程文件归档
│   ├── 搜索/   (搜索中间结果)
│   ├── 爬取/   (原始爬取)
│   ├── 清洗/   (清洗中间态)
│   ├── 标签/   (标签最终版)
│   └── 调试/   (CDP/Feed调试)
└── 参考/                        ← 关键词表
```

## 今日产出

| 文件 | 规模 |
|------|------|
| 全量清洗 | 663 条视频，6,819 条评论 |
| 近三年 | 575 条，5,889 评 |
| 三年前 | 88 条，930 评 |
| 精选版 | 349 条（中高相关），279 负面评论 |

## 新增脚本

| 文件 | 用途 |
|------|------|
| `source/cleandata/fetch_comments_only.py` | 独立评论拉取 |
| `source/cleandata/clean_douyin.py` | 基础清洗 |
| `source/cleandata/clean_douyin_v2.py` | 深度清洗+去无关 |
| `source/cleandata/validate.py` | 数据严谨性校验 |
| `source/cleandata/final_output.py` | 规范输出+汇总 |
| `source/cleandata/summarize.py` | 汇总表生成 |
| `source/cleandata/analyze_dimensions.py` | 关键词维度分析 |
| `progress.py` | 实时进度监控 |

## 修改文件

| 文件 | 改动 |
|------|------|
| `source/douyin_crawler/spiders/search.py` | CDP 搜索复用+重试（`_search_cdp`, `search_multi` 重构） |
| `source/cleandata/douyin_labeler.py` | 修复评论截断 bug（`build_output` 1-5→1-20，补字段，列名取全量） |

## 已知问题

1. 搜索 API 仍被风控，需 CDP 浏览器（每次使用前启动 Chrome debug 模式）
2. 数据量比第一轮少（663 vs 1087）是因为去掉了旧爬取数据中的重复+低质内容
3. 播放数字段始终为 0（抖音 API 不返回）
