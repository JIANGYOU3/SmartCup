# 工作日志 — 2026年7月7日

## 概述

抖音数据采集系统全面升级：从零搭建 CDP 浏览器搜索 → 爬虫字段扩展 → AI 多维标签 → 关键词分类表 → 批量关键词设计，形成完整的数据闭环。

## 时间线

### 09:00 - 12:00 | 爬虫字段升级
- **需求**：抓取作者粉丝量、视频收藏数、评论详情（含点赞数/回复数/子回复）
- **实施**：
  - `items.py`：新增 `author_follower_count`、`collect_count`、`DouyinComment` 结构化评论类
  - `douyin.py`：`fetch_comments()` 返回结构化 `DouyinComment` 列表，`scrape()` 提取 statistics.collect_count 和 author.follower_count
  - CSV 输出从 12 字段扩展为 14 基础字段 + 评论展开列（90 列）
- **测试**：Feed API → 详情 API → 评论 API，全部字段验证通过（粉丝量 1,085,801 / 收藏数 3,963 / 评论点赞数 4,945）
- **提交**：`148b239`

### 12:00 - 14:00 | CDP 浏览器搜索
- **问题**：搜索 API (`/aweme/v1/web/general/search/single/`) 返回 `verify_check` 风控
- **根因分析**：a_bogus + websign 签名均正常，风控是账号/IP 级别
- **解决方案**：Chrome DevTools Protocol (CDP) 浏览器搜索
  - 利用 Chrome `--remote-debugging-port=9222` 开启调试模式
  - Python 通过 WebSocket 连接，在浏览器中执行 `fetch()` 调用搜索 API
  - 浏览器内置环境自动处理 Cookie/Headers/签名
- **WSL2 ↔ Windows 联调**：独立 Chrome profile (`ChromeDebug`)、`--remote-allow-origins=*` 解决 WebSocket 403
- **提交**：`180242c`、`5d5f379`

### 14:00 - 16:00 | 数据采集
- **第一轮**：关键词"智能水杯"，20 页，147 条视频
- **第二轮**：8 关键词扩展（智能水杯/恒温杯/智能保温杯/控温水杯/55度杯/电热水杯/华为水杯/小米水杯），721 条视频
- **合计**：868 条视频，包含完整评论数据
- **提交**：-

### 16:00 - 18:00 | AI 多维标签分类
- **设计**：8 维标签体系
  - 内容标签（26 类品牌/产品/类型）
  - 内容相关性（高/中/低 + 理由）
  - 话题类别（测评/开箱/广告/教程/对比/娱乐/资讯）
  - 用户情绪（正面/负面/中性/混合）
  - 需求痛点（从评论提取用户真实需求）
  - 数据价值（高/中/低 + 理由）
  - 关键词（3-5 个）
  - 一句话总结
- **管道**：合并多轮 CSV → 去重 → 清洗无效评论（2.8%）→ DeepSeek API 6 并发打标签 → CSV + Excel
- **结果**：766 条完成标签，标签分布：
  - 智能水杯/温控杯 254 条 | 保温杯推荐/导购 215 条 | 电热水杯 178 条
  - 广告 35% | 测评 21% | 正面情绪 30% | 高相关性 57%
- **提交**：`a48bfa5`

### 18:00 - 19:00 | 关键词分类表
- **需求**：从标签数据生成结构化关键词库，用于后续爬虫搜索
- **实施**：`generate_keywords.py`
  - 从 766 条标签提取 2,847 个原始关键词
  - 规则分类 + DeepSeek AI 优化
  - 9 类关键词：核心词/品牌词/功能词/场景词/评价词/人群词/痛点词/长尾词/推荐搜索组合
  - 输出 126 精选关键词 + 10 个搜索组合
- **提交**：`4ceb8a4`

### 19:00 - 21:00 | 批量关键词设计 + CDP 优化
- **读取用户 Excel**：`抖音水杯_视频评论区爬虫关键词设计.xlsx`
  - 37 个关键词组，5 大维度（A舆情认知/B喝水习惯/C温度传感/D硬件痛点/E场景人群）
  - 127 个唯一搜索关键词
- **CDP antispam 修复**：浏览器 fetch 缺少 `sec-ch-ua`/`sec-fetch-*` 安全头被反爬识别
  - 添加完整浏览器 headers 后恢复
- **限流问题**：今日搜索请求过多，触发 `antispam_check`/`hit_limit`，需冷却数小时
- **提交**：`eebcb6e`

### 21:00 | 文档更新
- README 标题：数据采集 · AI 分析 · 关键词洞察
- 新增 CDP 搜索完整教程（Chrome 配置/命令/排错）
- 新增 douyin_labeler 一键管道文档
- 新增关键词表生成文档
- 更新管道流程图
- **提交**：`b1729a6`

## 今日提交

| # | Hash | 内容 |
|---|------|------|
| 1 | `148b239` | feat: 抖音爬虫新字段——作者粉丝量+收藏数+结构化评论 |
| 2 | `180242c` | feat: CDP 浏览器 fetch API 搜索 + referer 编码修复 |
| 3 | `e22d71a` | docs: README 添加 CDP 浏览器搜索完整教程 |
| 4 | `5d5f379` | feat: 搜索排序支持 — 综合/最多点赞/最新发布 |
| 5 | `a48bfa5` | feat: 抖音数据清洗 + DeepSeek AI 多维标签分类 |
| 6 | `4ceb8a4` | feat: 关键词提取 & 分类表生成 |
| 7 | `b1729a6` | docs: README 更新完整工作流——抖音多维标签+关键词表 |
| 8 | `eebcb6e` | fix: CDP搜索添加完整浏览器安全头，修复 antispam 拦截 |

**共 8 个 commit，16 个文件变更，+1,733 / -259 行**

## 数据产出

| 文件 | 内容 | 规模 |
|------|------|------|
| `爬取结果_合并.csv` | 两轮爬取合并 | 868 条视频 |
| `爬取结果_清洗后.csv` | 清洗无效评论 | 868 条，移除 2.8% 无效评论 |
| `标签结果.csv/.xlsx` | AI 8维标签 | 766 条，41 列 |
| `关键词表.csv/.xlsx` | 9 类关键词 | 126 精选 + 10 搜索组合 |

## 已知问题

1. **搜索 API 限流**：今日搜索请求过多触发 `antispam_check`，已修复 CDP headers 但需冷却数小时
2. **RENDER_DATA SSR 策略失效**：抖音已改为纯客户端渲染，策略 A 不可用
3. **CDP 需手动启动 Chrome**：每次使用前需 PowerShell 开启调试模式

## 明日计划

1. 运行 127 关键词全量搜索 + 爬取（限流恢复后）
2. 按 Excel 关键词组维度分析数据
3. 整合知乎 + 抖音两个平台数据做交叉分析
4. 如有需要，接入评论情绪编码表（Sheet 2）
